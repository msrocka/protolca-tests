{
  "name": "protolca-tests",
  "version": "0.0.1",
  "description": "A test suite for the gRPC service interface of openLCA",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "msrocka",
  "license": "MPL-2.0",
  "dependencies": {
    "@grpc/grpc-js": "^1.2.12",
    "@grpc/proto-loader": "^0.5.6",
    "mocha": "^8.3.2"
  }
}
